---
- name: Prepare
  hosts: all
  become: true

  tasks:
    - name: Install gfortran (Debian/Ubuntu)
      ansible.builtin.apt:
        name: gfortran
        state: present
        update_cache: true
      when: ansible_facts['os_family'] == 'Debian'
